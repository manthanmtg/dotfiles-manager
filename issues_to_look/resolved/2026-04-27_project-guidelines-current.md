# project guidelines are current

- Date: 2026-04-27
- Selected prompt: `project_guidelines_sync.md`
- Traceability evidence:
  - `package.json` scripts match the command list in `AGENTS.md` and `CLAUDE.md`.
  - `src/app/api` contents match the route structure described in `AGENTS.md` and include platform guards where applicable.
  - Dotfiles directory and prompt set structure align with `AGENTS.md`.
  - Existing guidance in `AGENTS.md`/`CLAUDE.md` matches this repository’s local-only, shell-focused workflow.
- Decision: project guidelines are current; no updates to AGENTS/CLAUDE/prompt files required.
