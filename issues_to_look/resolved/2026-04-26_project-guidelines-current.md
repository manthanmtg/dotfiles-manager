# project guidelines are current

- Date: $(date +%Y-%m-%d)
- Selected prompt: `project_guidelines_sync.md`
- Traceability evidence:
  - `AGENTS.md` command list matches `package.json` scripts.
  - `src/` and `dotfiles/` directory structure matches architecture documented in `AGENTS.md`.
  - API route files in `src/app/api/` all include `assertSupported()` guards.
  - Prompt set reflects current repository files and safe scope.
- Decision: No guideline edits required at this time.
